<%= form_with(model: zone_history, local: true) do |form| %>
  <% if zone_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zone_history.errors.count, "error") %> prohibited this zone_history from being saved:</h2>

      <ul>
        <% zone_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task %>
    <%= form.text_field :task %>
  </div>

  <div class="field">
    <%= form.label :zone_id %>
    <%= form.text_field :zone_id %>
  </div>

  <div class="field">
    <%= form.label :employee %>
    <%= form.text_field :employee %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
